module CommentsHelper
  def render_thread(comments, css_class)
    #last_level = 0
    #comments.each do |comment|
    #  if comment.level >= last_level
    #    tag :div, {:class => css_class}
    #  end
    #  last_level = comment.level
    #end
  end
end
